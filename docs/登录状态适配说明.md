# 登录状态适配增强说明

**更新日期**: 2026-04-08  
**问题**: 登录后和登录前，抢购按钮可能有不同，脚本需要处理这两种状态

---

## 🎯 问题描述

智谱 AI 页面在用户**登录前**和**登录后**可能呈现不同的 DOM 结构：

### 登录前状态
- 可能显示"登录"按钮
- 购买按钮可能被隐藏或禁用
- 按钮文本可能是"请登录"或完全不显示

### 登录后状态
- 显示"立即抢购"、"提交订单"、"特惠订阅"等按钮
- 按钮可用（非 disabled 状态）
- 可能直接弹出支付窗口

---

## ✅ 已实施的解决方案

### 1. 增强选择器配置

在 `src/auto-buy-glm-pro-optimized.js` 中扩展了 `SELECTORS` 配置：

```javascript
const SELECTORS = {
  // 新增：登录前按钮选择器
  loginButtons: [
    'button:has-text("登录")',
    'a:has-text("登录")',
    '.login-btn',
    '[ref*="login"]',
    'text=请登录',
  ],
  
  // 扩展：包含更多登录后按钮文本
  subscribeButtons: [
    // ...原有选择器...
    'button:has-text("立即抢购")',
    'button:has-text("提交订单")',
    'button:has-text("Pro 连续包月")',
  ],
};
```

### 2. 增强 `clickProSubscribeButton()` 函数

**新增功能**:
- ✅ **点击前检测登录状态** - 在尝试点击前检查当前是否已登录
- ✅ **未登录提示** - 如果未登录，记录详细日志并提示用户
- ✅ **查找登录按钮** - 尝试定位登录按钮位置，帮助用户快速登录
- ✅ **按钮状态记录** - 记录按钮文本、是否禁用等详细信息

**代码片段**:
```javascript
async function clickProSubscribeButton(page) {
  // ========== 阶段 0: 检测当前登录状态，适配按钮策略 ==========
  const isLoggedIn = await checkIfLoggedIn(page);
  logger.debug('当前登录状态', { isLoggedIn }, 'CLICK');
  
  if (!isLoggedIn) {
    logger.warning('检测到未登录状态，按钮可能不可用', {}, 'CLICK');
  }
  
  // ...原有点击策略...
  
  // 策略 5: 如果未登录，尝试查找登录按钮并提示用户
  if (!isLoggedIn) {
    try {
      const loginButtons = [
        'button:has-text("登录")',
        'a:has-text("登录")',
        '.login-btn',
        '[ref*="login"]',
      ];
      
      for (const selector of loginButtons) {
        const loginBtn = page.locator(selector).first();
        if (await loginBtn.isVisible()) {
          logger.warning('找到登录按钮，但需要用户手动登录', {
            selector,
          }, 'CLICK');
          break;
        }
      }
    } catch (e) {
      // 忽略登录按钮查找错误
    }
    
    logger.warning('当前未登录，请手动登录后脚本会继续执行', {}, 'CLICK');
  }
  
  return false;
}
```

### 3. 增强 `attemptSubscribe()` 函数

**新增功能**:
- ✅ **点击前二次登录检查** - 库存可用时再次确认登录状态
- ✅ **等待登录机制** - 如果未登录，最多等待 30 秒让用户手动登录
- ✅ **登录后刷新页面** - 登录成功后刷新页面确保 DOM 更新
- ✅ **登录后库存复检** - 刷新后重新检查库存状态（可能已变化）
- ✅ **点击失败原因分析** - 记录点击失败时是否因为未登录

**代码片段**:
```javascript
async function attemptSubscribe(page, attemptNum) {
  // ...库存检查...
  
  if (stockStatus === 'AVAILABLE') {
    // ========== 增强：点击前再次检查登录状态 ==========
    const preClickLoggedIn = await checkIfLoggedIn(page);
    if (!preClickLoggedIn) {
      logger.warning('点击前检测到未登录，等待用户手动登录...', {
        attemptNum,
      }, 'AUTH');
      
      // 等待登录（最多等待 30 秒）
      let waitCount = 0;
      const maxWaitCount = 15; // 15 * 2s = 30s
      while (!await checkIfLoggedIn(page) && waitCount < maxWaitCount) {
        waitCount++;
        if (waitCount % 3 === 0) {
          logger.info('等待登录中...', { attemptNum, waitCount });
        }
        await sleep(2000);
      }
      
      if (waitCount >= maxWaitCount) {
        logger.error('等待登录超时', { attemptNum, maxWaitTime: '30 秒' }, 'AUTH');
        return false;
      }
      
      // 登录后刷新页面确保 DOM 更新
      await page.reload({ waitUntil: 'networkidle', timeout: 10000 });
      await sleep(1000);
      
      // 重新检查库存状态（登录后可能变化）
      const postLoginStockStatus = await checkStockStatus(page);
      if (postLoginStockStatus !== 'AVAILABLE') {
        logger.warning('登录后库存状态变化', {
          attemptNum,
          from: 'AVAILABLE',
          to: postLoginStockStatus,
        }, 'STOCK');
        return false;
      }
    }
    
    // 点击订阅按钮
    const clicked = await clickProSubscribeButton(page);
    
    // ========== 增强：点击失败后检查是否因为登录状态变化 ==========
    if (!clicked) {
      const postClickLoggedIn = await checkIfLoggedIn(page);
      if (!postClickLoggedIn) {
        logger.warning('点击失败：检测到未登录状态', {
          attemptNum,
        }, 'AUTH');
      }
    }
    
    // ...后续逻辑...
  }
}
```

---

## 📊 改进效果

### 改进前
- ❌ 假设用户始终已登录
- ❌ 未登录时直接报错或找不到按钮
- ❌ 无法处理登录状态变化
- ❌ 日志信息不足，难以诊断问题

### 改进后
- ✅ **自动检测登录状态** - 每次尝试前都检查是否登录
- ✅ **智能等待登录** - 未登录时自动等待用户手动登录（最多 30 秒）
- ✅ **登录后自动恢复** - 刷新页面、重新检查库存，确保状态最新
- ✅ **详细日志记录** - 明确记录登录状态、按钮文本、失败原因
- ✅ **多选择器策略** - 支持登录前后的多种按钮文本和状态

---

## 🔍 日志示例

### 场景 1: 未登录时库存释放

```
[10:00:01.234] [SUCCESS] [STOCK] 🎉 库存已释放！
[10:00:01.236] [WARNING] [AUTH] 点击前检测到未登录，等待用户手动登录...
[10:00:03.240] [INFO] [AUTH] 等待登录中... (第 3 次，已等待 6 秒)
[10:00:07.456] [SUCCESS] [AUTH] 检测到已登录，继续点击...
[10:00:08.567] [INFO] [NAVIGATION] 刷新页面获取最新库存状态...
[10:00:09.678] [INFO] [CLICK] 开始点击 Pro 套餐订阅按钮
[10:00:09.789] [SUCCESS] [CLICK] ✓ 点击 Pro 连续包月按钮成功
```

### 场景 2: 登录后按钮状态变化

```
[10:00:02.123] [DEBUG] [CLICK] 当前登录状态：{ isLoggedIn: true }
[10:00:02.234] [DEBUG] [CLICK] 检查 Pro 连续包月按钮
  - selector: '[ref="e401"]'
  - isVisible: true
  - buttonText: "立即抢购"
  - isDisabled: false
[10:00:02.345] [SUCCESS] [CLICK] ✓ 点击 Pro 连续包月按钮成功
  - buttonText: "立即抢购"
  - clickDuration: 111ms
```

### 场景 3: 点击失败因未登录

```
[10:00:03.456] [WARNING] [CLICK] 所有点击策略均失败
  - totalDuration: 1234ms
  - isLoggedIn: false
[10:00:03.567] [WARNING] [AUTH] 当前未登录，请手动登录后脚本会继续执行
```

---

## 🧪 测试建议

### 测试场景 1: 未登录启动
1. 退出智谱 AI 账号
2. 运行脚本：`npm start`
3. 观察日志是否提示"未登录状态"
4. 手动登录账号
5. 观察脚本是否自动继续执行

### 测试场景 2: 已登录启动
1. 确保已登录智谱 AI
2. 运行脚本：`npm start`
3. 观察日志是否显示"检测到已登录状态"
4. 确认脚本正常执行抢购流程

### 测试场景 3: 登录超时
1. 退出账号
2. 运行脚本
3. 等待 30 秒不登录
4. 观察日志是否显示"等待登录超时"

---

## 📝 相关文件

- `src/auto-buy-glm-pro-optimized.js` - 主要修改文件
- `src/auto-buy-logger.js` - 日志模块（无需修改）
- `docs/抢购失败分析指南.md` - 故障排查参考

---

## ⚠️ 注意事项

1. **等待时间限制**: 未登录时最多等待 30 秒，超时后本次尝试失败
2. **登录后刷新**: 登录成功后会自动刷新页面，确保 DOM 状态最新
3. **库存复检**: 刷新后会重新检查库存，防止登录后库存已售罄
4. **日志级别**: 建议使用 DEBUG 级别查看详细登录状态变化

---

## 🎯 下一步优化建议

1. **延长等待时间** - 如果 30 秒不够，可增加 `maxWaitCount` 配置
2. **自动识别登录页面** - 检测是否跳转到登录页并自动导航
3. **记住登录状态** - 配置 `USER_DATA_DIR` 避免每次手动登录
4. **多账号支持** - 支持多个账号轮流尝试

---

*本次更新确保了脚本在登录前/登录后各种状态下的鲁棒性，提高了真实环境下的成功率。*
